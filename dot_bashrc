# Sourcing ble.sh at the top of ~/.bashrc
if [[ $- == *i* ]]; then
  # Load ble.sh if it exists
  [[ -f ~/.local/share/ble.sh/ble.sh ]] && source ~/.local/share/ble.sh/ble.sh
fi

eval "$(starship init bash)"
